generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "mysql"
    url      = env("DATABASE_URL")
}

model Admin {
    email    String @unique
    password String
}

model Image {
    id  Int    @id @default(autoincrement())
    src String @unique
}
